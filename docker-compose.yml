version: "3.9"

services:
  orac:
    build: .
    container_name: orac
    volumes:
      - ./models/gguf:/models/gguf:ro
      - ./third_party/llama_cpp:/app/third_party/llama_cpp:ro
      - ./logs:/app/logs
    environment:
      - LOG_LEVEL=INFO
      - LOG_DIR=/app/logs
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
    restart: unless-stopped
    networks:
      - orac-network

networks:
  orac-network:
    name: orac-network
